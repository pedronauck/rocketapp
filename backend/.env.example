# Backend Environment Example

# Server
PORT=3005

# AI Provider & Model
# Choose provider: openai (default) or groq
PROVIDER=openai

# OpenAI API key (required if PROVIDER=openai)
OPENAI_API_KEY=sk_your_key_here

# Groq API key (required if PROVIDER=groq)
GROQ_API_KEY=gsk_your_key_here

# Optional model override (defaults to openai/gpt-oss-20b)
# Note: openai/gpt-oss-20b is supported when using Groq’s OpenAI-compatible endpoint.
AI_MODEL=openai/gpt-oss-20b
# Optional: abort slow generations/streams (ms)
AI_TIMEOUT_MS=20000

# Public URL (for Twilio)
# Set to your ngrok domain WITHOUT scheme, e.g., abcd1234.ngrok-free.app
NGROK_URL=

# Optional: Greeting spoken at call start
RELAY_WELCOME_GREETING=Hi! I am a voice assistant powered by Twilio. Ask me anything!

# Optional: Override WebSocket URL used by Twilio ConversationRelay
# Example (dev/local): ws://localhost:3005/twilio/relay
RELAY_WS_URL=

# Optional: Use poke-mcp over SSE for Pokémon facts/tools
# Example: http://localhost:3000/sse
POKE_MCP_SSE_URL=

# Optional: Validate Twilio webhooks (recommended in prod)
TWILIO_AUTH_TOKEN=
TWILIO_ACCOUNT_SID=
TWILIO_VERIFY_SERVICE_SID=

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# Optional: Custom system prompt for the assistant
# Important: NEVER use Markdown or code formatting; plain text only.
# Avoid emojis, bullet points, and special characters that can break Twilio voice.
SYSTEM_PROMPT=You are a friendly Pokédex Call Center agent. Answer in 1-3 concise sentences. Never use Markdown; plain text only. Avoid emojis, bullets, and special characters.
