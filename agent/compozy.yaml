name: pokemon-vision
version: "0.1.0"
description: Image-based Pokémon recognition using a vision tool and agent orchestration

author:
  name: Compozy
  url: https://github.com/pedronauck

workflows:
  - source: ./workflow.yaml

models:
  - provider: openai
    model: gpt-4o-mini
    api_key: "{{ .env.OPENAI_API_KEY }}"
    default: true

runtime:
  type: bun
  entrypoint: "./entrypoint.ts"
  permissions:
    - --allow-read
    - --allow-net
    - --allow-write
